package org.example;

import org.example.components.Gui;

/**
 * This is the main entry point of the application.
 * It initializes the graphical user interface (GUI) component.
 */
public class Main {
    public static void main(String[] args) {
        var gui = new Gui();
    }
}